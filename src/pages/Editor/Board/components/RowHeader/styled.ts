import styled from "styled-components";
export const StyledHeader = styled.div`
  overflow: hidden;
  display: inline-block;
  position: absolute;
  top: 50px;
  bottom: 0;
  padding-bottom:20px;

`;
